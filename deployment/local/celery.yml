---
execution:
    config:
        broker: pyamqp://dagplusplus:dagsterwithconcurrencyisprettysweet@0.0.0.0:5672/dagster-broker  # Use the broker container hostname
        backend: rpc://  # Or use a different backend like Redis
